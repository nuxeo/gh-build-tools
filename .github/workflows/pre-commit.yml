---
name: Pre-commit

on:
  pull_request:
    branches:
      - main
  pull_request_review:
    types: [submitted]
  push:
    branches:
      - main

jobs:
  pre-commit:
    # Trigger a run when approving a dependabot PR or as usual
    if: >-
      (github.event.review.state == 'approved' && github.event.pull_request.user.login == 'dependabot[bot]') ||
      (github.actor != 'dependabot[bot]' && github.event_name != 'pull_request_review')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - uses: Alfresco/alfresco-build-tools/.github/actions/github-https-auth@v18.8.0
        with:
          username: ${{ vars.PLATFORM_BOT_USERNAME }}
          pat: ${{ secrets.PLATFORM_BOT_TOKEN }}

      - uses: Alfresco/alfresco-build-tools/.github/actions/pre-commit@v18.8.0
        with:
          auto-commit: "true"
