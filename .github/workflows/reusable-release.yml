name: Release

on:
  pull_request:
    types:
    - closed
  workflow_dispatch:
    inputs:
      release_type:
        description: "Release type"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
    release:
        name: Release
        if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
        uses: Alfresco/alfresco-build-tools/.github/workflows/reusable-release.yml@3ee3eb0795e69b00ff305ea57ac05d0d04dc8d36 # v18.11.0
        with:
          release_type_override: ${{ inputs.release_type }}
          commit_username: ${{ vars.PLATFORM_BOT_USERNAME }}
        secrets:
          BOT_GITHUB_TOKEN: ${{ secrets.PLATFORM_BOT_TOKEN }}
