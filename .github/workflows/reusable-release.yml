name: Release

on:
  pull_request:
    types:
    - closed
  workflow_dispatch:
    inputs:
      release_type:
        description: "Release type"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
    release:
        name: Release
        if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
        uses: Alfresco/alfresco-build-tools/.github/workflows/reusable-release.yml@9b9b46aa72bb76bec1b35c4a2aea4459b8f5a3cd # v12.4.3
        with:
          release_type_override: ${{ inputs.release_type }}
          commit_username: ${{ vars.PLATFORM_BOT_USERNAME }}
        secrets:
          BOT_GITHUB_TOKEN: ${{ secrets.PLATFORM_BOT_TOKEN }}
