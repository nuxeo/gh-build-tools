name: Release

on:
  pull_request:
    types:
    - closed
  workflow_dispatch:
    inputs:
      release_type:
        description: "Release type"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
    release:
        name: Release
        if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
        uses: Alfresco/alfresco-build-tools/.github/workflows/reusable-release.yml@a6a8be7429080c132815d0482a55ae4fd28f15ba # v18.7.0
        with:
          release_type_override: ${{ inputs.release_type }}
          commit_username: ${{ vars.PLATFORM_BOT_USERNAME }}
        secrets:
          BOT_GITHUB_TOKEN: ${{ secrets.PLATFORM_BOT_TOKEN }}
